library(DBI)
## Warning: package 'RMySQL' was built under R version 4.3.3
library(glue)
## Warning: package 'DBI' was built under R version 4.3.3
library(RMySQL)
library(DBI)
## Warning: package 'RMySQL' was built under R version 4.3.3
library(glue)
## Warning: package 'DBI' was built under R version 4.3.3
library(RMySQL)
## Warning: package 'DBI' was built under R version 4.3.3
library(dplyr)
## Warning: package 'DBI' was built under R version 4.3.3
library(readr)
# 1. Konfigurasi Database
db_config <- list(
host = "127.0.0.1",
port = 3306,
user = "root",
password = "",
dbname = "projecttransactiondatabase"
)
# 2. Koneksi ke MySQL Server (Tanpa Database Spesifik)
con <- dbConnect(
MySQL(),
host = db_config$host,
port = db_config$port,
user = db_config$user,
password = db_config$password
)
library(DBI)
## Warning: package 'RMySQL' was built under R version 4.3.3
library(glue)
## Warning: package 'DBI' was built under R version 4.3.3
library(RMySQL)
## Warning: package 'DBI' was built under R version 4.3.3
library(readr)
# 1. Konfigurasi Database
db_config <- list(
host = "127.0.0.1",
port = 3307,
user = "root",
password = "",
dbname = "projecttransactiondatabase"
)
# 2. Koneksi ke MySQL Server (Tanpa Database Spesifik)
con <- dbConnect(
MySQL(),
host = db_config$host,
port = db_config$port,
user = db_config$user,
password = db_config$password
)
# 3. Hapus Database Jika Sudah Ada (Untuk Mencegah Konflik)
tryCatch({
dbExecute(con, glue("DROP DATABASE IF EXISTS {db_config$dbname}"))
message("Database lama dihapus")
}, error = function(e) {
message("Gagal menghapus database: ", e$message)
})
# 4. Buat Database Baru
tryCatch({
dbExecute(con, glue("CREATE DATABASE {db_config$dbname}"))
message("Database ", db_config$dbname, " berhasil dibuat")
}, error = function(e) {
message("Gagal membuat database: ", e$message)
})
library(DBI)
library(RMySQL)
library(glue)
library(dplyr)
library(readr)
# Konfigurasi Database
db_config <- list(
host = "127.0.0.1",
port = 3307,
user = "root",
password = "",
dbname = "projecttransactiondatabase"
)
# Koneksi ke MySQL Server (Tanpa Database Spesifik)
con <- dbConnect(
MySQL(),
host = db_config$host,
port = db_config$port,
user = db_config$user,
password = db_config$password
)
# Hapus Database Jika Sudah Ada
tryCatch({
dbExecute(con, glue("DROP DATABASE IF EXISTS {db_config$dbname}"))
message("Database lama dihapus")
}, error = function(e) {
message("Gagal menghapus database: ", e$message)
})
# Buat Database Baru
tryCatch({
dbExecute(con, glue("CREATE DATABASE {db_config$dbname}"))
message("Database ", db_config$dbname, " berhasil dibuat")
}, error = function(e) {
message("Gagal membuat database: ", e$message)
})
# Koneksi ke Database yang Baru Dibuat
con_db <- dbConnect(
MySQL(),
host = db_config$host,
port = db_config$port,
user = db_config$user,
password = db_config$password,
dbname = db_config$dbname
)
# Definisi Tabel
table_definitions <- list(
cabang = """
CREATE TABLE IF NOT EXISTS cabang (
ID_Cabang VARCHAR(100) PRIMARY KEY,
ID_Produk INT,
kota VARCHAR(100),
jumlah_stock INT
) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
""",
# Koneksi ke Database yang Baru Dibuat
con_db <- dbConnect(
MySQL(),
host = db_config$host,
port = db_config$port,
user = db_config$user,
password = db_config$password,
dbname = db_config$dbname
)
# Definisi Tabel
table_definitions <- list(
cabang = """
CREATE TABLE IF NOT EXISTS cabang (
ID_Cabang VARCHAR(100) PRIMARY KEY,
ID_Produk INT,
kota VARCHAR(100),
jumlah_stock INT
) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
""",
library(DBI)
library(RMySQL)
library(glue)
library(dplyr)
library(readr)
# Konfigurasi Database
db_config <- list(
host = "127.0.0.1",
port = 3307,
user = "root",
password = "",
dbname = "projecttransactiondatabase"
)
# Koneksi ke MySQL Server (Tanpa Database Spesifik)
con <- dbConnect(
MySQL(),
host = db_config$host,
port = db_config$port,
user = db_config$user,
password = db_config$password
)
# Hapus Database Jika Sudah Ada
tryCatch({
dbExecute(con, glue("DROP DATABASE IF EXISTS {db_config$dbname}"))
message("Database lama dihapus")
}, error = function(e) {
message("Gagal menghapus database: ", e$message)
})
# Buat Database Baru
tryCatch({
dbExecute(con, glue("CREATE DATABASE {db_config$dbname} CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci"))
message("Database ", db_config$dbname, " berhasil dibuat")
}, error = function(e) {
message("Gagal membuat database: ", e$message)
})
# Koneksi ke Database yang Baru Dibuat
con_db <- dbConnect(
MySQL(),
host = db_config$host,
port = db_config$port,
user = db_config$user,
password = db_config$password,
dbname = db_config$dbname
)
# Definisi Tabel
table_definitions <- list(
cabang = """
CREATE TABLE IF NOT EXISTS cabang (
ID_Cabang VARCHAR(100) PRIMARY KEY,
ID_Produk INT,
kota VARCHAR(100),
jumlah_stock INT
) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
""",
